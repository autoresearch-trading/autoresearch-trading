# Research State — legacy v1 closed archive pointer

This file used to contain the detailed state for the old v1 representation-learning branch. That branch is closed and is no longer the active starting point.

Do not use this file as live state for current work.

Active source of truth now:

1. `CLAUDE.md`
2. `docs/NEXT_SESSION_HANDOFF.md`
3. `docs/AGENT_OPERATING_MAP.md`

Historical details are preserved in git history and in the historical experiment docs under `docs/experiments/`, `docs/archive/`, and `docs/superpowers/specs/`.
